const registerUser = require('./registerUser.service');
const loginUser = require('./loginUser.service');
const invest = require('./invest.service');

module.exports = {
  registerUser,
  loginUser,
  invest,
};
