const getTransactionDetails = require('./getTransactionDetails.service');
const updateTransaction = require('./updateTransaction.service');

module.exports = {
  getTransactionDetails,
  updateTransaction,
};
